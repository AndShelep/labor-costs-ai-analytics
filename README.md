# Відкриті дані AI Analytics

## Мета проекту

Аналіз офіційних державних статистичних даних щодо витрат підприємств на оплату праці в Україні (2005–2019) з метою виявлення регіональних відмінностей, довгострокових тенденцій та структурних змін з часом. Отримання навичок використання системи контролю версій git.

## Джерело даних

Офіційна Державна служба статистики України
Портал відкритих даних: https://data.gov.ua/dataset/d83c20f1-c2a1-4fb8-96f7-4ec9788ab41b

Набір даних: Витрати підприємств на оплату праці за регіонами України (2005–2019).

## Дослідницькі питання

1. Чи існує довгострокова тенденція зростання витрат на оплату праці протягом 2005–2019 років?

2. Витрати на утримання робочої сили значно відрізняються між регіонами. Де найбільші та найменші витрати?

3. Чи вплинули економічні кризи (2008–2009 та 2014–2015 років) на динаміку витрат на оплату праці?

## Етап завантаження даних

Створено гілку feature/data_load. Створено файл data_load.py та записник data_load.ipynb у яких було прочитано дані та висвітлено перші 5 рядків

## Етап перевірки якості даних

Створено гілку feature/data_quality_analysis. Було досліджено розмір та структуру даних. Обреблено пропущені значення.

## Етап дослідження

1. Було виявлено довгостроковий зростаючий тренд
2. Виявлено значні відмінності у витратах на утримання робочої сили у різних областях України у 2019 році
3. Значний спад середніх витрат на утримання робочої сили у кризові роки

Також було спрогнозовано приблизну кількість витрат по усій Україні на 1 рік (2020)

## Етап візуалізації

Додано код для побудови та відображення графіків результатів, отримані на етапі дослідження


# Інструкція запуску

## Вимоги

- Docker Desktop

Кроки для запуску:

```bash
У терміналі вводяться наступні команди:

docker --version
docker compose version

docker compose up --build

Для ручного запуску кожного модуля окремо потрібно послідовно виконати наступні команди:
docker run --rm -v ${PWD}/data:/app/data -v ${PWD}/db:/app/db -v ${PWD}/artifacts:/app/artifacts labor-data-load
docker run --rm -v ${PWD}/data:/app/data -v ${PWD}/db:/app/db -v ${PWD}/artifacts:/app/artifacts labor-data-quality
docker run --rm -v ${PWD}/data:/app/data -v ${PWD}/db:/app/db -v ${PWD}/artifacts:/app/artifacts labor-data-research
docker run --rm -v ${PWD}/data:/app/data -v ${PWD}/db:/app/db -v ${PWD}/artifacts:/app/artifacts labor-visualization
docker run --rm -p 5000:5000 -v ${PWD}/data:/app/data -v ${PWD}/db:/app/db -v ${PWD}/artifacts:/app/artifacts labor-web